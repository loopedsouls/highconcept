{
  "name": "highconcept",
  "version": "1.0.0",
  "description": "High Concept Game - A web-based game where you manage a convenience store with a smile mask.",
  "main": "index.html",
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist/css dist/js dist/assets && cp index.html dist/ && cp -r css/* dist/css/ && cp -r js/* dist/js/ && cp -r assets/* dist/assets/ 2>/dev/null || true",
    "dev": "http-server . -p 3000 -o",
    "start": "http-server dist -p 3000"
  },
  "keywords": ["game", "web", "javascript"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "http-server": "^14.1.1"
  }
}